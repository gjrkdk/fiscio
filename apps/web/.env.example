# ─── Supabase ────────────────────────────────────────────────────────────────
# Vind je in: supabase.com → project → Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<anon-key>

# ─── Database (Supabase Session Pooler) ──────────────────────────────────────
# Gebruik port 5432 (session pooler) voor drizzle-kit push/migrate
# Gebruik port 6543 (transaction pooler) voor runtime queries
# Speciale tekens in wachtwoord URL-encoden: @ → %40, ? → %3F, $ → %24
DATABASE_URL=postgresql://postgres.<project-ref>:<password>@aws-1-eu-central-1.pooler.supabase.com:6543/postgres

# ─── E-mail (Resend) ─────────────────────────────────────────────────────────
# Aanmaken op resend.com (gratis tier: 3.000 e-mails/maand)
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxx
# Eigen domein vereist voor productie; test via onboarding@resend.dev
RESEND_FROM_EMAIL=facturen@jouwdomein.nl

# ─── AI (OpenAI) ─────────────────────────────────────────────────────────────
# Gebruikt voor: bonnetjes OCR (gpt-4o-mini vision) + rit-classificatie
# Aanmaken op: platform.openai.com → API Keys
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxx

# ─── Cron beveiliging ────────────────────────────────────────────────────────
# Willekeurige string — beveiligt /api/cron/* routes tegen ongeautoriseerde aanroepen
# Vercel stuurt deze automatisch mee als Authorization: Bearer <secret>
# Stel ook in via: Vercel → Settings → Environment Variables
CRON_SECRET=<willekeurige-geheime-string>

# ─── Site URL ────────────────────────────────────────────────────────────────
# Lokaal:     http://localhost:3000
# Productie:  https://fiscio.vercel.app  (of eigen domein)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
