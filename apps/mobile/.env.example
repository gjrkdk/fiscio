# ─── Supabase (publieke keys — veilig in Expo app bundle) ────────────────────
# Vind je in: supabase.com → project → Settings → API
EXPO_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=<anon-key>
